package com.github.kramerev3axr.discoords.client;

import net.minecraftforge.client.gui.overlay.IGuiOverlay;

public class CoordsHudOverlay 
{
	public static final IGuiOverlay HUD_COORDS = (((gui, poseStack, partialTick, screenWidth, screenHeight) -> 
	{
//		Minecraft.getInstance().font.draw(poseStack, "Coordinates Database", screenWidth / 2 - (256 / 2), screenHeight / 2 - (256 / 2), 0x000000);
	}));
}
